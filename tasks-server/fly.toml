# fly.toml
app = "tasks-server"

[build]
  builder = "heroku/buildpacks:20"

[env]
  NODE_ENV = "production"

[[services]]
  internal_port = 3000
  protocol = "tcp"
  [[services.ports]]
    port = 80

[processes]
  app = "pnpm start:prod"